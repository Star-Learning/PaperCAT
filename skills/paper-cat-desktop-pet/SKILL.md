# PaperCat Desktop Pet

This local skill mirrors the MVP instructions in `paper-cat-desktop-pet-skill.md`.

Use it to keep PaperCat focused on the first complete flow:

PDF drop -> cat state animation -> FastAPI PDF parsing -> OpenAI-compatible summary -> SQLite history -> result window.

